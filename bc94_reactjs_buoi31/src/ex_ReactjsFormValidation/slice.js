import { createSlice } from "@reduxjs/toolkit";

const initialState = {
    listArr: []
};

const manageStudentSlice = createSlice({
    name: "manageStudentSlice",
    initialState,
    reducers: {

    }
});

export default manageStudentSlice.reducer;